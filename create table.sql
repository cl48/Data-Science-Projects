CREATE TABLE dataset1 (
	user_id varchar(255),
	date date,
	hour integer,
	impressions integer,
	clicks integer,
	video_midpoint integer,
	video_complete_views integer,
	asset varchar(255),
	conversions integer
);

CREATE TABLE dataset2 (
	user_id varchar(255),
	gender varchar(255),
	age integer,
	device_type varchar(255),
	os varchar(255),
	nrs_grade varchar(255),
	date_collected date
);

